{% extends 'base.html.twig' %}

{% block body %}

    <section class="container-fluid py-5">
        <div class="col-lg-3 col-md-8 mx-auto">
          <h1 class="fs-light text-center">
              {{ user }}
          </h1>
            <p class="lead text-muted">
                welkom bij het dierenrijk
            </p>
        </div>
    </section>

    <table class="table">
        <thead>
        <tr>
            <th class="col">
                Dier
            </th>
            <th class="col">
                Beschrijving
            </th>
            <th class="col">
                landnaam
            </th>
        </tr>
        </thead>
        <tbody>
        {% for verhalen in beschrijving %}
        <tr>
       <td>
           {{ verhalen.name }}
       </td>
            <td>
                {{ verhalen.description }}
            </td>
            <td>
                {{ verhalen.user.part }}
            </td>
            <td>
{#                <a href="{{ path('update' , {{ id:verhalen.id }}) }}"></a>#}
            </td>
        </tr>
        {% endfor %}
        </tbody>
    </table>

{% endblock %}